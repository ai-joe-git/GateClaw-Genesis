# Core dependencies
# Note: These are minimal - the system works without them in placeholder mode

# Vector operations (optional - uses simple cosine similarity if not installed)
# numpy>=1.24.0

# Embedding model (optional - uses hash-based pseudo-embeddings if not installed)
# sentence-transformers>=2.2.0

# Vector database (optional - uses SQLite if not installed)
# chromadb>=0.4.0

# NLP utilities (optional - uses simple heuristics if not installed)
# spacy>=3.7.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Development
black>=23.0.0
mypy>=1.5.0